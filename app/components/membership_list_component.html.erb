<% stacked = (@memberships.count > 5) %>
<%= tag.ul class: ['text-left flex', stacked ? ' -space-x-1' : ''].join(' ') do  %>
  <% @memberships.each do |membership| %>
    <%= tag.li() do %>
      <%= user_avatar_image(membership.user, 30, 'rounded-full') %>
    <%end%>
  <% end %>
<% end %>
