<%= render NarrativeComponent.new(object: comment, parent: @parent) do %>
   <% if comment.content.nil? and @form.nil? %>
      <%= link_to "Add a object here...", new_polymorphic_path([comment.commentable, comment]), class: "block border h-12 px-4 rounded" if comment.content.nil? %>
   <% else %>
    <%= comment.content %>
   <% end %>
<%end %>
