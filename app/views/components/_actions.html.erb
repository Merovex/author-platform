<%# if can? :edit, obj %>
    <%= link_to "Edit #{obj.model_name}", edit_polymorphic_path(obj), class: t('css_action_link') %>
    <% if obj.is_a? Book %>
    <%= link_to "Add Praise", new_book_praise_path(obj), class: t('css_action_link') %>
    <%end%>
<%# end %>
