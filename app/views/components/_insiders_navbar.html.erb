
<nav class='bg-yellow-400 dark:bg-yellow-500 text-black text-center sticky bottom-0 md:top-0 z-60 flex items-center space-x-8 justify-center'>
  <%= link_to "Public Site", root_path, role: 'menuitem', class: 'hover:underline mx-2' %>
  <%= link_to "Dashboard", dashboard_path(), role: 'menuitem', class: 'hover:underline mx-2' %>

  <%= link_to notifications_path(), role: 'menuitem', class: 'inline-flex relative w-fit' do %>
    <%= bi_icon('bell-fill') %>
    <%= notifications_badge() %>
  <%end%>
  
  <%= link_to "My Profile", edit_user_registration_path, role: 'menuitem', class: 'hover:underline mx-2' %>
  <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: button_css('brand') if user_signed_in? %>
</nav>