
<nav class='bg-yellow-300 text-black text-center sticky bottom-0 md:top-0 z-60 flex items-center space-x-8 justify-center'>
  <%= link_to "Public Site", root_path, role: 'menuitem', class: 'hover:underline mx-2' %>
  <% if can? :admin, Post %>
    <%= link_to "Home", dashboard_path(), role: 'menuitem', class: 'hover:underline mx-2' %>
    <%= link_to "Notifications", user_notifications_path(Current.user) %>
  <% end %>
  
  <%= link_to 'Me', edit_user_registration_path, class: 'hover:underline mx-2' %>
  <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: button_css('brand') if user_signed_in? %>
</nav>