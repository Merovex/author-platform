<div class="<%= t('css_article') %>">
<%= headline('Editing Page: '+@page.title) %>

<%= render "form", page: @page %>

<br>

<div>
  <%= link_to "Show this page", @page %> |
  <%= link_to "Back to pages", pages_path %>
</div>
