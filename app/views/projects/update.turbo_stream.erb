<%= turbo_stream.replace('project-form', partial: 'projects/turbo_form', locals: { project: @project }) %>
<%= turbo_stream.replace('flash', partial: 'shared/flash', locals: { project: @project }) %>
